# StampDraft Recovery and Phase-by-Phase Execution Plan

**Prepared:** 2026-07-19
**Repository:** `/Users/mac/Documents/workspace/Projects/stampdutycalc`
**Branch:** `watchdog/multistate-foundation`
**Recovery base:** `22842b4`; the completed work is being published as the reviewable
commit series on `watchdog/multistate-foundation`.

This is the authoritative resume plan for the interrupted work. It reconciles the
calculator handoff, Watchdog handoff, source-coverage matrix, saved terminal output,
current working tree, and fresh verification runs.

## Live continuation ledger

**Updated:** 2026-07-22

This ledger supersedes older counts in the historical stopping-point section below.
Update it after every verified implementation slice so a new terminal can resume from
the first unchecked item without reconstructing intent from chat history.

Current verified checkpoint:

- Phase 1A.1 complete: modifier attachments are required temporal dependencies;
- Phase 1A.2 complete: generic version-family and transitive-availability validation;
- Phase 1A.3 complete: explicit fail-closed request policy with local-only draft opt-in;
- Phase 1A.4 complete: every MVP rule version receives a shared, date-aware input
  contract; runtime and structural validation enforce it, and the web form derives its
  fields from the same registry;
- Phase 1A.5 complete: caller-authored Maharashtra legal conclusions were replaced
  with observable inputs and hashed internal derivations;
- Phase 1A.6 complete: eligibility and arithmetic fixtures are separate;
- Phase 1A.7 complete: canonical active-law snapshots are archived by hash before
  audit insertion, and replay uses only the validated archive rather than live rules;
- Phase 1A.8 complete: memo rendering requires a short-lived, signed, firm-scoped
  capability issued only after a scoped record lookup; no direct record-ID or encoded
  matter-data memo path remains;
- Phase 1A platform safety invariants are complete;
- Phase 1B.1 complete: deterministic prose-doubt inventory, CI drift gate, Delhi joint
  transfer-duty warning/refusal coverage, and Karnataka surcharge/gift refusals;
- Phase 1B.2 complete: Delhi mortgage/ATS amendment and input boundaries, partnership
  low-capital boundary, and globally refused LLP classification are explicitly covered;
- Phase 1B.3 complete: Maharashtra metro/LBT/gift/mortgage locality controls and both
  surcharge-active eras are covered, including the previously open 2019–2020 interval;
- Phase 1B.4 complete: all 83 executable pending controls have exact stable-ID coverage
  dispositions and severity drift checks. Phase 1B exit criteria are satisfied.
- Phase 2.1 complete: source acceptance is derived solely from a deterministic promotion
  report; Maharashtra Part IV-B passes the exact-range gate and legacy Part 8 remains
  provisional pending its second complete identical run.
- Phase 2.2 complete: Watchdog rejects zero-row success, bounds response bodies while
  streaming, enforces redirect hosts before following them, integrity-checks resume
  checkpoints, records metadata revisions append-only, and sanitizes recorded requests.
- Phase 2.3 complete: Maharashtra Part 8 passed its second exact complete sweep with
  195/195 documents independently re-fetched, zero new blobs/occurrences, and generated
  promotion to accepted status.
- Phase 2.4 complete: deterministic manifests cover all 2,742 Maharashtra blobs; local,
  metadata-only, partial, corrupt, backup, and restore states are executable and tested.
- Phase 2 implementation is complete. A real off-device provider and successful restore
  remain an explicit release blocker, not a fabricated local success.
- Phase 3.1 complete and hardened: deterministic Maharashtra triage selects 529
  occurrences / 526 blobs from 6,433 archived occurrences, including every Part 8
  enacted-law title as a backstop; body extraction produced page-level keyword receipts
  for 491 text PDFs, recorded 35 empty/image-only PDFs and zero failures, and kept every
  candidate explicitly non-citable.
- Phase 3.2 complete: Part 8 has receipt-bound, non-overlapping query coverage from the
  2015 product floor through its accepted baseline; Part IV-B has complete 2015–2020
  query runs and 207/208 early-2021 documents. The exact inaccessible 2021-05-28 row is
  a machine-reported document gap, not concealed as completeness.
- Phase 3.3 legal review advanced: official Maharashtra Acts III of 2021, VII of 2022,
  XXXII of 2024, and IX and XX of 2025 were pinpointed in the immutable Part 8 archive;
  append-only eras now encode Article 40(b) from both 9-Feb-2021 and 20-Jan-2022,
  Article 54 from 20-Jan-2022, the 31-Jul-2024 section 39 penalty fork, and the four
  14-Oct-2024 rate changes.
- Phase 3.3 remission safety advanced: exact Part IV-B orders dated 1-Jan-2026 and
  22-May-2026 expose agricultural/crop-loan and GCC Policy remissions. Every affected
  calculator path presently encoded under Articles 25, 36, 36A, 40, and 54 now requires
  an explicit order-scope answer and refuses eligible or uncertain cases; other listed
  order articles remain outside the numeric corpus. No remission percentage was inferred
  into arithmetic.
- Phase 3.3 charging repair complete: Act XX of 2025 proves that section 4's
  ancillary-instrument amount changed from Rs 100 to Rs 500 on 1-Apr-2025. The former
  single Rs 500 era is split at the exact boundary, with day-before/day-of coverage.
- Phase 3.3 section 9 safety advanced: locally archived orders demonstrate targeted,
  retrospective, industrial, housing, public-project, logistics, agricultural, and GCC
  relief. Article 25, 36, and 40 paths now collect an observable other-remission claim
  screen and refuse if a party claims relief or a project/policy may qualify.
- Phase 4 safety floor advanced: current Karnataka bare Article 20 rate, Article 30
  lease/licence, and charging sections 4–6 now refuse from 2021 until the annual Act and
  commencement graph is complete. Their five controls are in the exact safety ledger.
- Phase 4 discovery/acquisition plumbing advanced offline: the DPAL 2021–2026 annual-page
  traversal and all nine required Act sentinels are fixture-tested; the live probe now
  invokes that full bounded traversal, and a source-specific sweep adapter verifies PDF
  signatures and archives every discovered row only after the checklist passes. No live
  fetch, committed acquisition, or completeness claim was made.
- Phase 5 discovery plumbing advanced offline: Delhi Revenue notification discovery now
  traverses bounded pagination, deduplicates stable rows, validates listing/PDF hosts,
  and discovers without downloading document bodies. Its live probe now invokes the
  complete bounded listing traversal while expressly reporting that PDFs were not
  acquired. A separate fixture-tested snapshot sweep acquires and receipts every unique
  visible PDF while requiring identical observation dates so it cannot masquerade as a
  historical date-range query. Live acquisition and history completeness are outstanding.
- Phase 6 release-candidate foundation advanced: `computeWithTrace()` exposes the exact
  input-dependent rule/cross-reference/applied-modifier/penalty graph without bypassing
  any computation gate. `assessComputationReleaseCandidate()` separately enforces
  catalog-valid current evidence, regression and refusal-neighbour receipts, and canary
  status for review readiness, then requires founder verification for production
  readiness. With 0/115 evidence links, no live corpus path is falsely marked ready.
- Phase 7 software gate complete: founder packets now freeze the encoded diff, exact
  evidence source rows/hashes/pinpoint locators, amendment and commencement chains,
  present-day/boundary/negative receipts, canary state, warnings, and open refusals.
  Packet generation cannot populate founder metadata and reports every current path as
  blocked before founder review while evidence remains 0/115 linked.
- Phase 8.4 complete: engine refusals carry stable remediation codes; both computation
  APIs return structured, backward-compatible 422 responses; and the form renders the
  precise input, legal-review, founder-verification, evidence, or invariant next step.
- Phase 8.5/8.6 advanced offline: core forms now expose explicit labels, pressed/live
  state, and skip navigation; baseline security headers are configured; production
  workspace failures no longer expose database connection details or local setup
  commands; and `BETA-RELEASE-CHECKLIST.md` records go/no-go metrics and rollback.
- Phase 8 measurement contract complete: validated beta observations calculate answer
  coverage and refusal rate across representative matters, but conditional correctness
  only across independently assessed answers. Refusals cannot enter the incorrect-answer
  count; canary divergence and audit/memo reliability are reported separately.
- Phase 9.1/9.2 provider-independent implementation complete: shared Tier 2 schemas enforce PDF/DOCX consent,
  60-page intake, 30-day or compute-and-delete retention, exact rule-contract fields,
  source-page/snippet receipts, mandatory per-field confirmation, and a one-way boundary
  that returns only confirmed engine inputs plus model version. Upload, status, draft,
  confirmation, deletion, expiry, and provider-error routes are implemented without
  claiming that an external provider is installed.
- Phase 9 provider-independent work complete: the memo has a genuine, searchable,
  multi-page PDF download protected by the same five-minute firm-scoped capability as
  HTML rendering. Tier 2 lifecycle receipts enforce India-region intent, compute-and-
  delete or 30-day policy, and atomic document/snippet deletion. A document-check UI
  requires explicit per-field review, carries confirmed values directly into computation
  and the immutable audit, and never persists proposed values or snippets in Postgres.
- Provider-neutral authentication now fails closed until a verified adapter is installed;
  exact issuer/subject memberships enforce firm isolation across matters, computations,
  replay, memos, and Tier 2. Forward-only checksummed migrations, privacy-safe liveness
  and readiness, a protected retention hook, and hashed transactional backup/clean-restore
  verification are implemented. Real IdP, India-resident extraction, encrypted off-device
  backup, production-Postgres drills, and a beta cohort remain external blockers.
- Phase 3/6 evidence packaging advanced: exact accepted-Gazette documents, independently
  recomputed hashes, extraction receipts, acquisition events, and gap-free audit intervals
  are assembled for Article 54 and its agricultural-loan and GCC remission screens.
  Thirteen deterministic proposals and 49 named golden-test receipts are mechanically valid and
  packaged for human review; production coverage deliberately remains 0/115 because no
  reviewer or founder metadata was populated.

Latest green baseline after the combined Phase 7–9 offline-safe implementation:

- TypeScript build: pass;
- rule validation: 0 errors, 0 warnings;
- evidence report consistency: pass, with 115 intentionally unlinked dependencies;
- evidence-link proposals: 13/13 mechanically valid, all pending human review;
- golden suites: 256/256 (216 eligibility, 40 arithmetic);
- focused corpus-safety suite: 1 file, 32 tests;
- focused Watchdog triage/integrity/hardening/promotion/evidence suite: pass;
- Vitest: 63 files, 367 tests;
- Next.js production build: pass;
- `git diff --check`: pass.

Phase 1A.4 checkpoint details:

- canonical contract schema and MVP registry: `packages/schema/src/input-contract.ts`
  and `packages/schema/src/mvp-input-contracts.ts`;
- loader attachment and full-era checks: `packages/engine/src/loader.ts` and
  `packages/engine/src/validators.ts`;
- pre-arithmetic enforcement: `packages/engine/src/input-contract.ts`, called by
  `compute()` after date-specific rule resolution;
- date-specific form derivation: `fieldsForRuleAtDate()` in `apps/web/lib/manifest.ts`;
- regression coverage: `packages/engine/src/input-contract.test.ts` and
  `apps/web/lib/manifest.test.ts`;
- production refusal remains unchanged; arithmetic-test mode cannot bypass malformed
  input rejection.

Phase 1A.5 completed family — Maharashtra service agreements:

- `RuleInputContract` now supports hashed `derived_facts` decision tables;
- callers answer `service_goods_transfer` and `service_additional_transaction`;
- the engine derives `agreement_scope` internally, and direct caller submission of
  that conclusion fails unknown-key validation;
- only two `no` answers reach the Article 5(h)(B) residual; every affirmative or
  uncertain path preserves the existing refusal;
- the works/service classification tree asks the same observable questions and routes
  uncertainty to escalation.

Phase 1A.5 completed family — Maharashtra POA:

- callers answer whether immovable transfer or developer/promoter powers exist and
  select the document's observable purpose;
- the engine derives `poa_scope`; direct caller submission is rejected;
- only quoted Article 48(a)-(e) purposes reach the flat rule;
- sale/developer branches, another purpose, uncertainty, and clause (h) remain refused;
- both historical rule eras and explicit user-facing refusal goldens are covered.

Phase 1A.5 completed family — Maharashtra lease/licence:

- `lease_value_scope` is derived from observable unentered-amount and unentered-renewal
  answers; either affirmative or uncertain answer refuses;
- `licence_payment_scope` is derived from whether the document has any payment absent
  from the entered fee/deposit fields;
- long-term leave-and-licence lease questions remain optional for short terms but the
  transitive Article 36 pending gate requires them for terms over 60 months;
- explicit eligibility goldens preserve both refusal paths.

Phase 1A.5 completed family — Maharashtra pre-2020 share transfer:

- the pre-1-July-2020 rule and contract now use statutory `share_value`;
- `share_base_scope` and its workaround refusal were removed;
- post-1-July-2020 remains date-specific `consideration`;
- date-specific UI and cross-state equality tests cover both eras.

Phase 1A.7 checkpoint details:

- `SnapshotArchive` in `packages/engine/src/snapshot.ts` is the canonical active-law
  payload already used by the historical hash, so existing `rules_version` identities
  do not change;
- archive parsing rejects malformed shapes, duplicate IDs, cross-jurisdiction content,
  and expected-hash mismatches before constructing an isolated replay `RuleSet`;
- `rules_snapshot_archive` stores canonical text once per hash and is append-only;
  database foreign-key/trigger enforcement prevents an audit row from preceding its
  archive, including on databases whose audit table predates the new foreign key;
- `recordComputation()` verifies the output/archive hash and archives before insertion;
- `/api/computations/verify` no longer imports or consults the live corpus and explicitly
  refuses missing, malformed, or hash-mismatched archives while preserving the strict
  request execution policy;
- regression coverage changes the live Delhi conveyance rule after archival and still
  reproduces the original canonical output from the stored snapshot;
- no `verified_on`, evidence link, or production-policy value changed. The corpus remains
  0/115 evidence-linked pending founder/legal verification.

Phase 1A.8 checkpoint details:

- `apps/web/lib/request-principal.ts` now requires a deployment authentication adapter;
  the default denies every protected request, and the adapter's issuer/subject identity
  must resolve to an existing membership inside the asserted firm;
- `apps/web/lib/memo-capability.ts` issues five-minute HMAC-SHA256 capabilities containing
  only opaque record/firm IDs, purpose, and bounded timing metadata;
- production and every non-loopback deployment require a server-only, 32-byte-or-longer
  `STAMPDRAFT_MEMO_CAPABILITY_SECRET`; only loopback development receives a fallback;
- `/api/computations/memo` performs a firm-scoped record lookup before issuing a token,
  then redirects with no-store/no-referrer headers;
- `/memo` accepts only the signed capability, rejects missing, expired, modified, or
  cross-firm tokens, and has no direct-ID or base64 matter-input fallback;
- focused tests cover signature tampering, wrong secrets, expiry, cross-firm use,
  unknown/malformed records, loopback configuration, and identifier-only payloads;
- route, repository, foreign-key, and end-to-end tests reject unauthenticated,
  non-member, and cross-firm access. Selecting and installing the production IdP adapter
  remains deployment work; the application no longer has a demo-principal fallback.

Phase 1B.1 checkpoint details:

- `packages/engine/src/corpus-safety-inventory.ts` dispositions all 48 matching prose
  fields by stable object/version/field key; it distinguishes machine refusals, machine
  warnings, non-amount exclusions, and unresolved legal-review items protected by a
  named refusal;
- `packages/engine/src/corpus-safety.test.ts` compares the exact key set rather than a
  count and resolves every declared control to its actual severity, so new prose-only
  doubts and fictional controls fail CI;
- Delhi joint conveyance before the hike and at/below Rs 25 lakh preserves the verified
  5% total but emits warnings for both provisional 2.5% components; the neighbouring
  joint cell above Rs 25 lakh refuses from 10-Jul-2023, while a current male cell remains
  callable in draft mode and a missing category refuses;
- Karnataka's secondary-only local surcharge now refuses every applied conveyance, and
  all gift totals refuse until family-cess and local-surcharge scope are sourced;
- arithmetic fixtures for those refused Karnataka formulas remain in the explicit
  arithmetic suite; two user-facing eligibility fixtures assert the new refusals;
- changed product/test files in this slice: `packages/engine/src/corpus-safety-inventory.ts`,
  `packages/engine/src/corpus-safety.test.ts`, the Delhi conveyance/transfer-duty corpus,
  the Karnataka gift/local-surcharge corpus, and two Karnataka golden files;
- no `verified_on`, evidence link, rate, or unsupported legal answer was populated. The
  current evidence blocker remains 0/115 linked.

Phase 1B.2 checkpoint details:

- mortgage-with-possession refuses immediately before, on, and well after 10-Jul-2023;
  missing `amount_secured` fails input validation first, while the explicitly principal
  mortgage-without-possession neighbour remains callable and collateral scope refuses;
- agreement-to-sell with possession refuses across the same date boundary and in the
  ambiguous window where consideration exceeds Rs 25 lakh but its 90% base does not;
  missing consideration/category inputs fail before legal evaluation, and the current
  no-possession residual remains closed;
- arithmetic-only fixtures pin the encoded mortgage and ATS threshold selection on the
  amendment day without presenting that encoding as a supported legal answer;
- Delhi partnership refuses at Rs 500, computes the neighbouring Rs 501 formula cell,
  rejects missing capital, and keeps dissolution distinct;
- LLP classification refuses globally at both low and ordinary contribution values,
  independently of the cross-referenced partnership branch;
- slice files: `packages/engine/src/corpus-safety.test.ts`,
  `golden/DL/share-transfer-mortgage-ats.json`, `golden/DL/bonds-partnership.json`, and
  `SOURCE-COVERAGE-MATRIX.md`; no rate, pending control, verification metadata, or
  evidence link changed.

Phase 1B.3 checkpoint details:

- a new date/location-scoped refusal closes metro conveyances from 8-Feb-2019 through
  31-Mar-2020, when the surcharge was active but no historical modifier existed;
- 7/8-Feb-2019, 31-Mar/1-Apr-2020, and 31-Mar/1-Apr-2022 boundaries prove the two active
  eras and the complete suspension interval without inventing a city list or rate stack;
- current conveyance is callable only with explicit non-metro and non-LBT facts; current
  metro, LBT-uncertain, and missing-locality cells refuse;
- all three gift relation branches compute only in an explicit non-metro/non-LBT location;
  favourable-family branches cannot bypass metro/LBT or missing-relation refusals;
- mortgage-with-possession is callable only for explicit non-usufructuary, non-metro,
  non-LBT facts; subtype, metro, LBT, and missing-fact neighbours refuse;
- slice files: `rules/MH/rules/conveyance.json`, the deterministic inventory and safety
  test, three Maharashtra golden files, and `SOURCE-COVERAGE-MATRIX.md`; no surcharge
  rate, city list, LBT scope, verification metadata, or evidence link was inferred.

Phase 1B.4 checkpoint details:

- `CORPUS_CONTROL_COVERAGE` inventories all 83 rule, modifier, charging, penalty, and
  classification-tree controls by stable object/version/index ID;
- every entry declares its runtime severity and its focused test/golden family or an
  explicit fail-closed dependency disposition;
- CI compares the exact control set, rejects duplicate/missing/renumbered controls, and
  verifies the declared `refuse`/`warn` severity against the parsed corpus;
- the existing exact 47-field prose inventory remains independent, so both new prose
  doubts and new machine controls require deliberate review;
- no additional callable gap was found after the Maharashtra 2019–2020 metro fix;
- Phase 1B exit criteria are met: known amount-affecting doubts are refused or expressly
  non-amount warnings/exclusions, current known-wrong paths do not emit user-facing
  figures, validators/goldens/tests are green, and the source matrix matches runtime.

Phase 2.1 checkpoint details:

- `packages/watchdog/src/sources.ts` no longer contains any handwritten acceptance
  status; `loadEvidenceCatalog()` attaches only the status emitted by
  `assessSourcePromotion()`;
- `WATCHDOG-PROMOTION-REPORT.json` is regenerated from committed indexes, sweeps, and
  events, and evidence validation fails when the artifact is missing or stale;
- every source report records its exact requested interval and floors, occurrence/blob
  counts, complete and incomplete runs, per-run additions, sentinel matches, and the
  replay/fixture tests required by the full CI gate;
- the report rejects missing contracts, wrong ranges/counts/pages/blobs, missing
  sentinels, too few complete repeats, later failed/partial runs, and a latest repeat
  that adds occurrences;
- Part IV-B passes with 2,575 unique occurrences, 2,547 unique blobs, three exact
  26-page runs, recovered historical partial/failed runs, the Mudrank sentinel, and
  zero latest-repeat additions;
- Part 8 is provisional despite its complete 195-document archive because only one
  exact successful run exists; no live sweep or source-status override was used.

Phase 2.2 checkpoint details:

- `HttpFetcher` now reads response streams incrementally, cancels immediately above the
  configured decoded-byte ceiling, follows redirects manually, and refuses non-HTTPS or
  non-allowlisted destinations before issuing the redirected request;
- the CLI passes each registry source's explicit host allowlist to live probes/sweeps;
- `runSweep()` rejects a zero-row adapter result before it can satisfy the old
  `0 covered === 0 listed` success condition;
- resumable rows still require a present blob whose recomputed SHA-256 matches the
  immutable occurrence index;
- metadata refreshes append a content-linked before/after record to
  `state/occurrence-history.jsonl` before updating the current occurrence locator;
- the response recorder strips credential-shaped URL parameters, userinfo, ASP.NET
  ViewState/EventValidation, hidden state, tokens, passwords, cookies, and similar form
  fields while retaining semantic search/postback values;
- the repeatable sanitizer updated 197 committed Part 8 sidecars: all 198 now pass the
  request-field audit, and metadata size fell from about 15 MB to about 196 KB without
  changing the committed replay HTML bodies.

Phase 2.3 checkpoint details:

- the official 2025-04-09 through 2026-07-16 listing again returned exactly 195 rows on
  two complete pages;
- all 195 documents were independently fetched rather than resume-skipped, and every
  body matched an existing immutable blob: 0 new blobs and 0 new occurrences;
- sweep `d7edfaed-607c-4e04-a493-75c057c812a5` is recorded as `ok`; the earlier bounded
  partial remains in the audit history and is marked recovered by the report;
- the regenerated report derives `accepted` for both Part 8 and Part IV-B; no registry
  status or verification metadata was edited.

Phase 2.4 checkpoint details:

- committed manifests bind each collection to its exact index SHA-256 and enumerate
  sorted unique blob hashes and byte lengths: 195 Part 8 and 2,547 Part IV-B;
- `validate-evidence.ts --require-blobs` verified all 2,742 local bodies (about 1.6 GB)
  by expected path, size, and streamed SHA-256;
- a metadata-only clone with no ignored bodies reports `unavailable`; any partial set,
  extra PDF, wrong size, wrong hash, stale index digest, or missing manifest fails;
- `DurableBlobStore` supplies provider-neutral upload/download operations, and the
  backup/restore test restores into a separate destination and re-verifies the manifest;
- `EVIDENCE-COVERAGE.md` records two manifest-covered sources and the explicit
  `NOT CONFIGURED — RELEASE BLOCKER` off-device status.

Phase 3.1/3.2 checkpoint details:

- stable identities are used by both Maharashtra adapters; the Part 8 migration retained
  195/195 occurrences and records its exact pre/post index digests;
- manifests now bind 1,361 Part 8 and 5,030 Part IV-B blobs;
- historical coverage is proved by exact range/row/page contracts and by matching each
  chosen sweep to its `document_acquired` hash receipt;
- Part 8 historical segments contain 1,173 occurrences across 11 complete queries;
- Part IV-B historical segments contain 2,490 occurrences across six complete yearly
  queries plus the 207/208 early-2021 partial;
- the sole Gazette document gap is stable row
  `473c8cdae40e7c93bb8268ad7614c9ed231e8536fb94445cb676f75fbafcf6ed`,
  dated 2021-05-28; bounded retries exhausted on the official delivery;
- the generated triage report lists the remaining source-family gaps: consolidated
  Act/Schedule spine, complete amendment/commencement chains, municipal/transport/LBT,
  concession/remission completeness, and IGR registration/valuation sources;
- no legal citation, `verified_on` value, or production eligibility changed; coverage
  remains 0/115 until complete chains and founder review exist.

Next uninterrupted slice — Phase 3.3 Maharashtra source spine and family acquisition:

1. Register and acquire a current first-party consolidated Act/Schedule spine with its
   consolidation date and authoritative historical baselines.
2. Add first-party IGR registration/valuation and municipal/transport/LBT source
   families without treating page probes or keyword hits as complete evidence.
3. Review the receipt-bound Gazette candidates into instrument-sized amendment and
   commencement chains; keep all calculator links closed until pinpoint human review.

Phase 3.3 progress:

- `mh-igr-publications` is registered as a provisional first-party departmental source;
  its registry description explicitly rejects reachability as proof of history/currency;
- source-specific navigation discovery recognizes Acts/Schedules, rules, notifications,
  government resolutions, circulars, valuation rates, fee tables, and reports, rejects
  zero recognized families, and refuses any discovered URL outside the official host;
- the probe reports entry-point/family counts with zero acquired documents, so it cannot
  be mistaken for evidence completeness;
- Gazette candidates now include title-derived publication stages, exact local blob
  paths, and sorted keyword pages: 409 enacted-law titles, 13 bills, 88 notification/order
  titles, and 17 other candidates. The enacted-law backstop prevents generic Act titles
  from bypassing review; all items remain review locators, not legal pinpoints;
- `WATCHDOG-MH-REVIEW-QUEUE.md` renders those locators as a deterministic 529-item
  checklist and is included in the evidence report staleness gate;
- Maharashtra Act IX of 2025 (published 1-Jan-2025 and deemed effective 14-Oct-2024)
  is pinpointed at immutable blob
  `d2e954fd3263b4b25faff58010dacd1941b64346a6d840d6863933d8220c6034`:
  page 2 changes Articles 4 and 5(h)(B) to Rs 500, and page 3 changes Article 47's
  ceiling to Rs 50,000 and Article 63 to a Rs 5 lakh threshold with a 0.3% rate;
- those four changes and both works/service classification consequences are encoded as
  append-only eras with amendment-day goldens; they remain unverified and unlinked,
  and the unresolved Article 63 cap interpretation still refuses cap-binding values;
- Maharashtra Act VII of 2022 is pinpointed at immutable blob
  `75e79d9e0328d6df2af370f582750c16f8b5867be159bb7db74cf452eb477db1`;
  pages 3-4 substitute Article 54's 0.1%/0.3% table, Rs 100 Article 40 proviso,
  Rs 20 lakh ceiling, and four exemptions from its 20-Jan-2022 publication;
- Article 54 now has append-only historical/current eras and observable exemption and
  proviso questions; current listed exemptions compute zero and every uncertain answer
  refuses, while production still requires the complete chain and founder review;
- Maharashtra Act III of 2021 is pinpointed at immutable blob
  `3ce0b0e5497d8a275fe9331343c841e9552b2445c715453e342f51ae19a3257f`;
  Article 40(b) now preserves its pre-9-Feb-2021 0.5% era, the Act III two-tier table
  with a Rs 10 lakh ceiling, and Act VII's 20-Jan-2022 Rs 20 lakh ordinary/Rs 50 lakh
  consortium ceilings; current uncertain mortgagee scope refuses;
- Maharashtra Act XXXII of 2024 is pinpointed at immutable blob
  `408bcde0f7740a5187da504c95ccd444874c7897e2022c229434aa0a8eaf00b2`;
  its 31-Jul-2024 section 39 change exposes a route-dependent penalty fork, so both
  pre-change and current route-free deficit outputs now refuse while arithmetic-only
  fixtures preserve the sections 31/34 formula;
- Maharashtra Act XX of 2025 is pinpointed at immutable blob
  `5cf517646e3cf2fa6b2a699b3356ee03ea86ae142ae4968d3e09f21034026caa`;
  section 2 changes the section 4 non-principal-instrument amount from Rs 100 to
  Rs 500 on 1-Apr-2025, and the charging corpus now preserves both exact eras;
- the 1-Jan-2026 Part IV-B agricultural/crop-loan order is pinpointed at immutable blob
  `ae5c7a68fd56f11193be7cdb6cc1a0b31ba7ffde3cef447d57986f32eee7d266`;
  affected current Article 40 and 54 paths refuse any eligible or uncertain case rather
  than overcharging an instrument potentially covered by the full remission;
- the 22-May-2026 Part IV-B GCC Policy order is pinpointed at immutable blob
  `ccc89c89bcb8cf960733da6f559c790a6b60644012fc33992840eae551c6e795`;
  its stated 3-Nov-2025 through 3-Nov-2030 policy period is date-gated across affected
  current paths, while all 50%/75%/100% zone/project branches remain refused pending an
  exact user questionnaire and complete source-chain review;
- representative targeted orders reviewed locally include the 12-Oct-2023 named public
  enterprise transfers, the 4-Jul-2025 retrospective Dharavi Article 36 remission and
  named Article 25 transfer, and the 17-Oct-2025 logistics-park remission. Rather than
  encode incomplete order arithmetic, a shared section 9 screen now closes Article 25,
  36, and 40 whenever other government relief is claimed or may apply;
- live IGR section traversal and document acquisition remain the next action; no IGR
  source has a promotion contract or accepted status.

Phase 4/5 safety and discovery progress:

- five Karnataka current-source-gap refusals close the previously callable bare Article
  20 rate, Article 30 lease/licence, and sections 4–6 from 1-Jan-2021 onward; historical
  formula coverage remains available only before that boundary or in explicit arithmetic
  fixtures;
- `discoverKarnatakaRequiredActs()` visits only the six direct DPAL annual pages for
  2021–2026, requires a same-year parsed group on every page, validates all discovered
  URLs against the source allowlist, and asserts Acts 26/2021, 11/2022, 12/2022,
  31/2022, 03/2023, 04/2024, 23/2024, 30/2025, and 42/2025;
- `probe ka-dpal-acts --live` now uses that full six-page traversal instead of the former
  single-year sample, and `sweep ka-dpal-acts` has a fixture-tested acquisition adapter
  that downloads, PDF-signature-checks, hashes, indexes, and receipts every discovered
  row only after the checklist succeeds;
- `discoverDelhiRevenueNotifications()` follows every discovered pagination URL under a
  configurable hard bound, canonicalizes the page-zero root, deduplicates rows by stable
  identity, and validates PDF URLs without downloading them;
- `probe dl-revenue-notifications --live` now runs that complete bounded pagination
  traversal and labels the result as link discovery, not document acquisition;
- `sweep dl-revenue-notifications` now provides a separate fixture-tested snapshot
  acquisition path with PDF-signature checks, immutable hashing, indexing, and receipts;
  it accepts only identical `from`/`to` observation dates and therefore cannot claim that
  the current listing covers an arbitrary historical interval;
- both traversals have deterministic multi-page fixtures. Network-restricted execution
  prevented live execution in this continuation, so no source status, document catalog,
  or evidence claim changed.
- promotion regression coverage proves that even a nominally successful Karnataka or
  Delhi acquisition remains provisional until an independently reviewed, machine-
  checkable source contract is registered; download success alone cannot confer accepted
  evidence status.

Phase 8.4 checkpoint details:

- `EngineError` now carries one of five coarse remediation codes while retaining its
  existing diagnostic message and defaulting unknown invariant failures to a safe generic
  refusal;
- pending-law, founder-verification, primary-evidence, and user-input gates assign their
  codes at the engine boundary, avoiding brittle API parsing of legal prose;
- `/api/compute` and `/api/computations` return `refusal` objects with stable title,
  message, next step, and retry semantics while preserving `escalation` for existing
  clients;
- the UI no longer gives universal s.6/s.31 advice for unrelated evidence or input
  failures; it displays the remediation belonging to the actual refusal class;
- focused tests execute all five classes through the real engine and compute route, and
  independently verify the source gate-to-code assignments.

Phase 8.5/8.6 and Phase 9 contract progress:

- computation, matter, and navigation controls now expose programmatic labels, selected
  state, busy/live-result state, and keyboard skip navigation without changing the
  computation payload;
- app-wide deployment headers deny framing, suppress referrers and MIME sniffing, and
  disable camera, microphone, and geolocation;
- workspace 503s and server-rendered setup failures expose one stable public message;
  database URLs, driver errors, and local Docker commands remain server/local-only;
- `BETA-RELEASE-CHECKLIST.md` separates conditional correctness from coverage, lists
  every current release blocker, and preserves append-only evidence/audit behavior in
  rollback;
- `packages/schema/src/extraction.ts` defines the provider-neutral Tier 2 intake,
  ephemeral extraction receipt, and confirmation boundary. Its tests cover consent,
  formats, page cap, exact contract membership, page receipts, edit/entry dispositions,
  required confirmation, snippet exclusion from confirmed output, India-region lifecycle
  receipts, retention deadlines, and atomic document/snippet deletion;
- `packages/schema/src/beta-metrics.ts` provides privacy-minimised beta aggregation with
  separate coverage, assessed-answer correctness, refusal, canary, Tier 2 edit, memo,
  and replay denominators;
- `apps/web/lib/memo-pdf.ts` and `/api/computations/memo/pdf` render a standalone PDF from
  the immutable audit record only after capability verification and a firm-scoped lookup;
- document upload/status/read/confirm/delete routes, a page-referenced confirmation UI,
  compute-and-audit handoff, a deterministic 95% precision harness, authenticated expiry,
  forward-only migrations, readiness, and database backup/restore are implemented and
  tested. Production IdP/storage/model/backup providers, labelled founder evaluation
  documents, production Postgres drills, and measured quality remain deployment or human
  inputs rather than invented local success.

Resume protocol:

1. Read this ledger and `git status --short`; preserve every pre-existing edit.
2. Run the narrow tests named in the latest ledger entry before changing code.
3. Continue through the next safe unchecked slices in order and run focused tests after
   each one; record external or human-only blockers without weakening their gates.
4. Run typecheck, validators, goldens, full Vitest, diff check, and the web build.
5. Record changed files, exact gate totals, unresolved blockers, and the next file/symbol
   here at every full-gate checkpoint.
6. Prepare reviewable commit boundaries. Commit and push only with explicit authorization;
   authorization to publish this branch was supplied on 2026-07-22. Never populate
   verification metadata without the qualified human review.

## 1. Historical stopping point (superseded by the live ledger)

The last committed checkpoint is `e64a13b` from 2026-07-17. Work continued for two
days without another commit. The working tree currently contains **60 entries**:

- 48 modified tracked files;
- 12 untracked files;
- no staged files;
- approximately 4,163 tracked-line additions and 194 deletions;
- approximately 1.8 GB of local, ignored Watchdog data.

The unfinished work is three coherent units:

1. **Maharashtra Part IV-B acquisition and identity repair.** A five-year corpus of
   2,575 source rows / 2,547 unique PDFs was acquired. Resumability, occurrence-based
   indexing, stable row identities, request limits, and a recorded identity migration
   were added. Part IV-B was then marked accepted.
2. **Transitive safety gates.** Cross-reference dependencies, modifiers, penalty
   regimes, charging sections, and classification trees were brought under pending
   and founder-verification gates. Known unsafe current paths now refuse.
3. **Watchdog-backed citation evidence.** Citation evidence schemas, catalog loading,
   production evidence gates, a 0/99 coverage report, and CI-oriented validation were
   added. The saved terminal stream disconnected immediately after the first wiring of
   this unit. Later edits completed most of it, but it was never checkpointed.

Fresh assessment on 2026-07-19:

| Gate | Result |
|---|---|
| TypeScript project build | Pass |
| Rule validation | Pass: 0 errors, 0 warnings |
| Golden cases | Pass: 166/166 |
| Vitest | Pass: 28 files, 178 tests |
| Next.js production build | Pass |
| `git diff --check` | Pass |
| Evidence validation | **Fail**: generated report differs only in final newline count |

The repository is therefore **nearly functional but not checkpoint-ready**. The full
gate is red until `EVIDENCE-COVERAGE.md` is regenerated exactly.

Production remains intentionally closed: all legal versions have `verified_on: null`
and the evidence report is **0/99 linked**. A passing draft test suite is not legal
verification.

## 2. Rules for uninterrupted execution

These rules apply to every phase:

- Work sequentially and prepare each phase as a focused commit-sized checkpoint.
- Do not merge or populate founder-verification fields without the qualified founder;
  branch publication is authorized separately and does not confer legal approval.
- Never weaken a refusal or evidence gate to make a test pass.
- If primary material does not settle a legal issue, preserve or add a scoped refusal
  and continue with the rest of the phase.
- Treat government sources as evidence; use secondary sources only to locate or
  challenge first-party material.
- Archive complete source result sets. Keywords are triage, not retention filters.
- Keep `Terminal Saved Output` as an untracked recovery artifact; do not include it in
  a product commit and do not delete or move it without approval.
- Do not begin non-MVP state acquisition. Gujarat, Tamil Nadu, Telangana, and Uttar
  Pradesh adapters may remain, but live expansion is parked until the three-state beta.
- Registration-fee depth, Tier 3 analysis, additional states, and a circle-rate database
  remain deferred under the current scope.

Execution should continue without asking for legal conclusions. The only mandatory
human stop is Phase 7, because founder review/merge is the act of verification. An
external credential, durable-storage destination, or repeatedly inaccessible official
source may also require one consolidated blocker report; none should cause unrelated
work to stop.

## 3. Ordered phases

### Phase 0 — Recover, review, and checkpoint the interrupted work

**Goal:** turn the current 60-entry worktree into reviewed, reproducible commits before
new research or encoding begins.

Tasks:

1. Regenerate `EVIDENCE-COVERAGE.md` byte-for-byte, including its final newline.
2. Add `validate:evidence` to the actual GitHub Actions job; it is currently present in
   `package.json` but absent from `.github/workflows/ci.yml`.
3. Review and fix recovery-specific correctness issues:
   - the `/classify` page is statically generated, so its server-derived “today” can
     freeze at build time;
   - application evidence dates need an explicit jurisdiction/time-zone policy instead
     of relying silently on UTC `toISOString()`;
   - the identity-migration record contains an ephemeral local backup path; replace
     that audit field with durable hashes/counts or another reproducible reference;
   - evidence interval validation currently expects one sweep to cover a whole chain;
     support a gap-free union of successful sweeps before annual source runs are linked;
   - catalog validation must account for identity migrations and must not treat a
     hardcoded `accepted` label as sufficient proof of promotion.
4. Reconcile conflicting documentation: current HEAD, dates, Part 8 second-run status,
   Part IV-B promotion basis, current test totals, and the fact that production coverage
   is 0/99.
5. Split the work into reviewable commits, in this order:
   - Part IV-B archive/resume/stable-identity implementation and evidence state;
   - transitive engine/refusal/verification safety unit;
   - citation-evidence schema, catalog, runtime gate, report, and CI gate;
   - coverage matrix, decisions, and handoff updates.

Exit criteria:

- full local gate passes: typecheck, rule validation, evidence validation, goldens,
  Vitest, diff check, and web production build;
- every new source/data claim has a regression or promotion check;
- working tree is clean except explicitly retained local/ignored evidence and the saved
  terminal recovery artifact;
- no push or merge has occurred.

### Phase 1A — Platform safety invariants

**Goal:** make the execution platform fail closed before relying on state-level
legal refusals.

This phase was inserted after an architecture review on 2026-07-19. It must finish
before the Delhi/Maharashtra/Karnataka audit resumes.

Tasks:

1. Treat every referenced modifier as a required temporal dependency. Runtime must
   throw if the referenced modifier is not active, and validation must prove complete
   coverage across the referring rule's entire half-open era.
2. Apply append-only overlap/gap validation to rules, modifiers, penalty regimes,
   charging rules, and classification trees. Validate cross-reference and tree-terminal
   availability across every relevant version boundary, not only `effective_from`.
3. Replace `NODE_ENV`-selected safety with an explicit execution policy that defaults
   to founder-verified plus evidence-backed execution. Draft mode must be a deliberate,
   local-only opt-in.
4. Add a per-rule input contract for required numeric values and observable categorical
   facts. Enforce non-negative money, positive integer terms, unknown-key rejection, and
   enum membership before evaluating a formula. Derive the UI manifest from the same
   contract so API and form inputs cannot drift.
5. Replace caller-supplied legal conclusions such as `*_scope=residual` with observable
   questionnaire answers and derived classifications.
6. Keep draft arithmetic fixtures separate from production eligibility/refusal fixtures,
   so a hard refusal cannot mask a decayed formula.
7. Archive canonical active snapshots by hash and replay a computation against its
   recorded snapshot rather than the current corpus.
8. Remove matter data from memo URLs. Use authenticated stored records or short-lived,
   opaque, server-resolved tokens; non-local memo rendering must not accept base64 input.

Exit criteria:

- missing or temporally inactive dependencies throw at runtime and fail validation;
- every versioned corpus object passes generic interval validation;
- safety is explicit and secure-by-default in every deployment mode;
- malformed, negative, unknown, and misspelled inputs are rejected before computation;
- arithmetic and eligibility suites both pass independently;
- historical replay loads the exact recorded snapshot hash;
- memo URLs contain no matter values or facts.

### Phase 1B — Finish the fail-closed legal safety audit

**Goal:** ensure every amount-affecting uncertainty is machine-enforced, not merely prose.

Current audit signal: the deterministic scan inventories 48 matching prose fields across
rules, modifiers, charging rules, penalties, and classification trees. Every field has an
exact-key disposition and every amount-affecting legal-review item names its runtime
refusal. Remaining work is branch-by-branch coverage and source resolution, not count-only
keyword triage.

Tasks:

1. Review every `PENDING`, `assumption`, `confirm`, and `unverified` note in rules,
   modifiers, charging rules, penalties, and classification trees.
2. For each issue that can change article, base, rate, cap, surcharge, concession,
   effective date, or payer liability, add a date/fact/branch-scoped refusal or remove
   the unsupported path from the callable corpus.
3. Prioritize Delhi joint and transfer-duty cells, mortgage base/scope, agreement-to-sell
   threshold, partnership low-capital clause, LLP classification, Maharashtra surcharge
   scope, and Karnataka gift/local-surcharge scope.
4. Add present-day, amendment-boundary, missing-fact, special-clause, and neighbouring
   refusal cases. Historical green cases must not substitute for current-date cases.
5. Add a deterministic safety inventory so new prose-only amount doubts cannot enter
   unnoticed.

Exit criteria:

- every amount-affecting open issue is linked to a refusal test or explicitly excluded;
- no current known-wrong path emits a number;
- validation, goldens, and tests remain green;
- the source-coverage matrix matches executable behavior.

### Phase 2 — Make Watchdog promotion and preservation trustworthy

**Goal:** make source acceptance a generated, reproducible result rather than a manual
status edit.

Tasks:

1. Complete local hardening: validated zero-row success, streaming response-size limits,
   redirect allowlist checks, resumable checkpoints, occurrence history, interruption
   tests, and secret-safe recorded fixtures.
2. Generate a promotion report per source covering range, pages, rows, unique occurrences,
   unique blobs, sentinel documents, second-run additions, failures, and replay results.
3. Derive `accepted` status from that report in validation; do not trust `sources.ts`
   alone.
4. Verify Part 8 with the required second complete identical run. Re-evaluate Part IV-B
   against the same generated gate after the identity migration.
5. Produce checksum manifests and restore-verification tooling for all blobs. Implement
   a provider-neutral durable-storage interface. Keep production closed until an actual
   off-device destination is configured and a restore succeeds.
6. Ensure a fresh clone can validate indexes and fixtures while reporting unavailable
   local blob bodies honestly.

Exit criteria:

- accepted sources pass a machine-generated promotion report;
- a failed, partial, drifted, stale, or unexplained zero-row sweep cannot establish
  freshness;
- identical replay/re-fetch adds zero occurrences and zero blobs;
- a checksum manifest exists and restore verification is executable;
- durable off-device storage remains an explicit release blocker until configured.

### Phase 3 — Complete Maharashtra’s MVP source graph and repair its rules

**Goal:** use the strongest existing source base to create the first review-ready state.

Tasks:

1. Maintain Part 8 coverage to the 2015 product floor and close the one exact Part IV-B
   early-2021 delivery gap using stable identities and polite resumable sweeps.
2. Establish a current Act/Schedule spine and complete amendment/commencement chains.
3. Add municipal/transport surcharge, city coverage, s.149A/s.149B stacking, LBT,
   concession, and relevant IGR source families.
4. Extract and pinpoint relevant instruments from the complete archive; do not attach a
   later or unrelated document merely because it mentions stamp duty.
5. Re-encode known-current failures as append-only eras: Articles 4, 47, 54, and 63,
   the works/service tree, and any transitive dependencies they affect.
6. Resolve or refuse metro-cess, mortgage, gift, women-concession, and locality branches.
7. Add immutable evidence links, current/boundary/negative goldens, and an instrument-sized
   founder-review packet for each repaired dependency group.

Exit criteria:

- Maharashtra source chains cover every review-ready active dependency through the
  chosen evidence date;
- known stale/wrong rules are repaired or remain hard refusals;
- evidence coverage increases only through exact, catalog-valid links;
- no verification metadata is populated before founder review.

### Phase 4 — Re-source Karnataka and rebuild the current eras

**Goal:** replace the five-year-stale source spine and remove the largest known error set.

Tasks:

1. Discover and archive DPAL annual material for 2021–2026.
2. Assert the known checklist: Acts 26/2021, 11/2022, 12/2022, 31/2022, 03/2023,
   04/2024, 23/2024, 30/2025, and 42/2025.
3. Locate and archive commencement notifications, especially for Acts 30/2025 and
   42/2025. Absence from an incomplete source must not imply non-commencement.
4. Add IGR and municipal/local-body source families for practice, surcharge, and scope.
5. Re-encode the missing Article 20 first-sale band and all applicable Act 04/2024
   changes, followed by later Acts and commencement effects.
6. Repair the penalty-route output contract and all affected POA, mortgage,
   hypothecation, affidavit, bond, partnership, LLP, works/service, and share-transfer
   paths.
7. Link exact evidence and add present-day/boundary/refusal goldens.

Exit criteria:

- the annual Act checklist and commencement graph are complete or affected paths refuse;
- no current Karnataka calculation depends on silence from the 2020 consolidation;
- local surcharge and family-gift cess are sourced or scoped out;
- review packets and all automated gates pass.

### Phase 5 — Complete Delhi’s source graph and narrow its supported paths

**Goal:** replace reproductions and assumptions with a current, first-party Delhi chain.

Tasks:

1. Acquire a current Schedule I-A or reconstruct it from official Delhi Gazette
   amendments with a complete history.
2. Traverse and archive the full Delhi Revenue notification history, including OCR
   records for scans.
3. Add the municipal source family for DMC/MCD s.147 and the 10-Jul-2023 transfer-duty
   notification.
4. Add recorded DORIS reconciliation canaries; treat them as anomaly signals, not law.
5. Establish source-backed scope for sale, gift, mortgage with possession, contract for
   transfer, perpetual lease, gender/joint treatment, base, threshold, and effective date.
6. Repair or refuse the low-capital partnership cell, LLP classification, lease/licence,
   POA forks, agreement-to-sell possession fork, works/service residuals, s.4, and penalty.
7. Link evidence and add present-day/boundary/negative goldens.

Exit criteria:

- every supported Delhi result has a complete first-party chain;
- reproduction-only or registrar-practice assumptions remain refused;
- DORIS divergences are visible and unexplained divergences block the affected path;
- review packets and all automated gates pass.

### Phase 6 — Close cross-state dependencies and produce release candidates

**Goal:** finish dependencies shared across state rules and make evidence coverage useful
at the actual computation-graph level.

Tasks:

1. Archive and link the central Stamp Act/securities chain: Finance Act 2019,
   commencement/rate notifications, and official guidance needed for physical share
   transfers.
2. Complete evidence for charging sections, penalty regimes, classification trees, and
   all actually applied modifiers—not only top-level Schedule rules.
3. Keep case-law monitoring separate from Gazette completeness; add pinpoint authorities
   only where licensing and first-party/public access permit.
4. Generate dependency-bounded release candidates. Each candidate must show its complete
   transitive rule graph, evidence hashes, checked intervals, current tests, refusal
   neighbours, and canary status.
5. Update `EVIDENCE-COVERAGE.md` and the source matrix deterministically after every batch.

Progress:

- exact computation dependency traces and the review/production readiness split are
  implemented and regression-tested;
- the builder carries evidence document hashes, amendment/commencement checked-through
  dates, test receipts, refusal neighbours, canary state, warnings, and founder status;
- missing or provisional evidence, stale chains, absent receipts, and unexplained canary
  divergence block review readiness. Founder verification remains a distinct Phase 7
  production gate.

Exit criteria:

- no release candidate contains a missing or invalid transitive evidence link;
- evidence intervals are current through the chosen release date;
- unsupported dependencies remain explicit coverage gaps, not silent fallbacks;
- full gate and production build pass from a clean checkout.

### Phase 7 — Founder verification and merge gate

**Goal:** perform the one human action the system must not automate.

Tasks:

1. Present one instrument-sized, dependency-bounded review batch at a time.
2. Include primary documents, hashes, pinpoint locators, amendment/commencement chain,
   encoded diff, present-day and boundary goldens, negative cases, and open refusals.
3. Founder either approves/merges, requests a correction, or leaves the path refused.
4. Populate `verified_by` / `verified_on` only as part of the approved merge workflow.
5. Re-run the complete production-mode dependency and evidence gates after every merge.

Exit criteria:

- approved paths have founder verification and valid current evidence;
- rejected/unresolved paths still refuse;
- production answers are enabled only for approved dependency-complete paths.

### Phase 8 — Independent validation and three-state beta readiness

**Goal:** test conditional correctness independently of the encoding workflow.

Tasks:

1. Spot-check high-value property, mortgage, lease/licence, works/service, and penalty
   matters against primary text and fixed official-portal canaries.
2. Investigate every divergence without changing rules merely to match a portal.
3. Exercise real Postgres workspace flows end to end: save, replay, verify, append-only
   audit protection, and setup failure behavior.
4. Test production refusal UX so a blocked path gives a clear, actionable explanation
   rather than a generic server failure.
5. Verify memo rendering, audit reproducibility, privacy/retention behavior, accessibility,
   and deployment configuration.
6. Measure answer coverage separately from conditional correctness.

Exit criteria:

- no unexplained canary divergence on supported paths;
- production-mode end-to-end scenarios pass;
- refusals are counted as coverage gaps, not wrong answers;
- beta release checklist and rollback procedure are complete.

### Phase 9 — Finish in-scope M4/M5 product work

**Goal:** complete the commissioned Phase 2 surface after the legal core is safe.

Tasks:

1. Build Tier 2 PDF/DOCX upload and extraction for only the required computation fields.
2. Require user confirmation/editing before any extracted value reaches the engine;
   preserve source snippets only for the document-retention lifecycle.
3. Add the founder-supplied, anonymized extraction evaluation set and meet the PRD’s
   precision gate on high-risk fields.
4. Add real authentication/firm isolation, real PDF memo export, production Postgres
   migrations/backups, and operational monitoring.
5. Schedule Watchdog only on a network proven to work with the government portals;
   automated runs create review artifacts, never rule changes.
6. Run a small three-state closed beta and measure coverage, escalations, memo exports,
   edit rates, and unexplained divergences.

Exit criteria:

- M4/M5 acceptance tests pass;
- privacy, retention, authentication, backup, and restore behavior are verified;
- beta metrics distinguish refusals from incorrect answers;
- Phase 3+ expansion remains parked pending beta evidence and a new brief.

## 4. Final completion definition

The current engagement is complete only when:

1. the interrupted work is safely checkpointed and all repository gates are green;
2. supported Delhi, Maharashtra, and Karnataka paths are founder-verified and linked to
   current immutable evidence across their complete dependency graphs;
3. unsupported paths fail closed with tested explanations;
4. evidence is durably preserved and restorable;
5. production web/workspace/memo flows pass end to end;
6. Tier 2 confirmation and Watchdog review workflows satisfy the current Phase 2 scope;
7. a closed beta can measure answer coverage without sacrificing conditional correctness.
